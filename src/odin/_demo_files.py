"""Embedded otest demo fixture — GENERATED, do not edit by hand.

Regenerate with: uv run python scripts/regen_demo_files.py <pristine-demo-dir>
Maps destination-relative paths to file content.
"""

from __future__ import annotations

# Empty queue subdirs every demo needs.
QUEUE_SUBDIRS = ("pending", "running", "done", "failed", "held", "carry")

FILES: dict[str, str] = {
    'CLAUDE.md': '# otest — Odin demo target project\n\nThis project exists to exercise Odin end-to-end. The product is a tiny\nPython CLI called `greeter` that prints greetings. Treat it as a\nthrowaway — the point is the workflow, not the code.\n\n## Stack\n\n- Python 3.11+, **uv-managed**. Never invoke the system Python.\n- Stdlib only. No runtime dependencies.\n- One package under `src/greeter/`.\n\n## This project is run by Odin\n\nTasks here are executed by Odin, a headless orchestrator. Odin injects the\ntask protocol automatically — how to end a task (the `<<<NEXT_CONTEXT>>>` /\n`<<<NEEDS_INPUT>>>` sentinel blocks) and how to phrase questions (as JSON).\nYou do not need to restate it; run `odin guide` to see the exact contract.\nThis file only carries the project\'s **workflow** rules below.\n\n- **Don\'t manage the queue.** Odin decides task order and feeds you the\n  context you need; never look for "the next task" yourself.\n- **Git: none for this demo.** It runs with `--no-git`; just edit files in\n  place — don\'t commit, branch, push, or open pull requests.\n\n## Workflow rules\n\n- **Test-to-green where tests exist.** If a task says "add tests,"\n  run them with `uv run pytest` before completing.\n- **No partial work in completion.** If you can\'t finish, ask a question\n  (`<<<NEEDS_INPUT>>>`) rather than half-landing and deferring.\n- **One task = one focused change.** Don\'t sneak in cleanups or\n  refactors that the task body didn\'t ask for.\n',
    'task.md': "# otest — task list overview\n\nThis is a human-readable index of the tasks queued in\n`queue/pending/`. Odin itself does not read this file; it only reads\nthe individual `NNN-slug.md` files in the queue. Keep this in sync by\nhand if you reshuffle.\n\nThe product being built is **greeter**, a trivial Python CLI that\nprints greetings. The point is to exercise Odin's queue, carry-context,\nand held-on-questions flows — not to build something useful.\n\n## Tasks\n\n| #   | Slug                    | What it does                                                                | Expected outcome     |\n|-----|-------------------------|-----------------------------------------------------------------------------|----------------------|\n| 001 | `init-pyproject`        | Write `pyproject.toml` for a package named `greeter`.                       | `<<<NEXT_CONTEXT>>>` |\n| 002 | `add-package-skeleton`  | Create `src/greeter/__init__.py` and a stub `__main__.py`.                  | `<<<NEXT_CONTEXT>>>` |\n| 003 | `add-hello-core`        | Create `src/greeter/core.py` with a pure `hello(name)` function.            | `<<<NEXT_CONTEXT>>>` |\n| 004 | `wire-cli`              | Wire `__main__.py` to argparse + `core.hello`.                              | `<<<NEXT_CONTEXT>>>` |\n| 005 | `add-greeting-styles`   | Intentionally underspecified — agent must ask which styles, flag, default.  | `<<<NEEDS_INPUT>>>`  |\n| 006 | `add-tests`             | Add `tests/test_core.py`, pin `pytest` as a dev dep, run green.             | `<<<NEXT_CONTEXT>>>` |\n| 007 | `add-readme`            | Write `README.md` documenting install, usage, and supported styles.         | `<<<NEXT_CONTEXT>>>` |\n\n## What to expect when you run it\n\n1. Tasks 001–004 complete cleanly. Each emits a `<<<NEXT_CONTEXT>>>`\n   block that becomes the prefix on the next task's prompt under\n   `## Context from previous task`.\n2. Task 005 is deliberately underspecified. The agent emits a question\n   (a JSON `<<<NEEDS_INPUT>>>` block) about the style set, the CLI surface,\n   and the unknown-style behaviour.\n3. At a terminal, Odin renders the question with options and a recommendation\n   and you answer inline; it then continues automatically. (Unattended/no\n   TTY, Odin instead writes `queue/held/005-add-greeting-styles.questions.md`\n   and exits — you fill in `## Answers` and run\n   `odin resume 005-add-greeting-styles`.)\n4. Tasks 005 (resumed), 006, and 007 then run to completion.\n\nTotal expected: 7 successful runs and one held → answered cycle.\n\nSee `readme.md` for the exact commands.\n",
    'readme.md': '# Running the otest demo through Odin\n\nThis project is a deliberately tiny target for exercising\n[Odin](../odin). It builds a throwaway Python CLI called `greeter` over\nseven queued tasks, one of which halts to ask you a question.\n\n## Prerequisites\n\n- `claude` on `PATH` (`claude --version` should work).\n- `odin` installed as a uv tool, pointed at the local Odin checkout:\n\n  ```sh\n  uv tool install --from ../odin odin\n  odin --version\n  ```\n\n  Re-install with `uv tool upgrade --from ../odin odin` after Odin\n  changes.\n- `uv` itself, for the tasks that touch `pyproject.toml` / `uv sync`.\n\n## Platform note (Claude Code fixture)\n\nThis demo ships with `CLAUDE.md` and is meant to be driven with Claude Code.\nPass `--platform claude` (or set `default_platform` / `$ODIN_PLATFORM`).\nIt does **not** include an `AGENTS.md` or exercise Cursor end-to-end.\n\nTo manually smoke-test the Cursor backend against this fixture (after\ncopying/adapting workflow into an `AGENTS.md`, and with Cursor\'s `agent`\nCLI on `PATH`):\n\n```sh\nodin run --platform cursor --no-git\n# or: odin run --platform cursor --agent-bin /path/to/agent --no-git\n```\n\nA future demo variant may ship an `AGENTS.md`; until then, keep the fixture\non Claude Code.\n\n## Layout you start with\n\n```\notest/\n├── CLAUDE.md          # sentinel protocol + workflow rules\n├── task.md            # human-readable index of what\'s queued\n├── readme.md          # this file\n└── queue/\n    └── pending/\n        ├── 001-init-pyproject.md\n        ├── 002-add-package-skeleton.md\n        ├── 003-add-hello-core.md\n        ├── 004-wire-cli.md\n        ├── 005-add-greeting-styles.md    <-- this one halts\n        ├── 006-add-tests.md\n        └── 007-add-readme.md\n```\n\nOdin will create `running/`, `done/`, `failed/`, `held/`, and `carry/`\nunder `queue/` as it runs.\n\n## Drive it\n\nFrom inside this directory:\n\n```sh\ncd otest\nodin run --platform claude\n```\n\nDefaults Odin uses:\n\n- `QUEUE_DIR` = `./queue`\n- `--project` = current working directory, so the agent\'s `cwd` is\n  `otest/` and it loads `otest/CLAUDE.md`.\n- `--permission-mode` = `acceptEdits` (Edit/Write auto-approve; Bash\n  still prompts unless you pre-approve).\n\nIf you want fully unattended runs (e.g. so task 006 can run\n`uv sync` and `uv run pytest` without prompting), pass an explicit\ntool allowlist:\n\n```sh\nodin run --platform claude --allowed-tools "Read,Edit,Write,Bash,Grep,Glob"\n```\n\nOnly do that because you\'ve read `CLAUDE.md` and trust the workflow.\n\n### Stopping early\n\nIf you only want to watch the first couple of tasks:\n\n```sh\nodin run --platform claude --max-tasks 2\n```\n\n### Dry run\n\n```sh\nodin run --platform claude --dry-run\n```\n\nLists the resolved platform + argv and prompt without invoking the agent.\n\n## What you\'ll see\n\n1. **Task 001–004** complete cleanly. After each one, Odin moves the\n   task file to `queue/done/` and drops a\n   `queue/carry/NNN-slug.next-context.md` file with the agent\'s\n   carry-forward prompt. The next task\'s invocation has that block\n   prepended under `## Context from previous task`.\n2. **Task 005 halts.** The agent emits `<<<NEEDS_INPUT>>>` because the\n   task body is intentionally underspecified — it doesn\'t name the\n   styles, the CLI surface, or the fallback behaviour. Odin will:\n\n   - move the task file to `queue/held/005-add-greeting-styles.md`,\n   - write `queue/held/005-add-greeting-styles.questions.md` with the\n     questions plus an empty `## Answers` heading,\n   - exit with code `10` and print the resume instructions.\n\n3. **You answer the questions.** Open the questions file and write your\n   answers under `## Answers`. Example:\n\n   ```sh\n   $EDITOR queue/held/005-add-greeting-styles.questions.md\n   ```\n\n   Something like:\n\n   ```markdown\n   ## Answers\n\n   1. Support three styles: `plain`, `formal`, `casual`.\n      - plain   → "Hello, {name}!"\n      - formal  → "Good day, {name}."\n      - casual  → "Hey {name}!"\n   2. Expose via `--style NAME` flag on the CLI; default is `plain`.\n   3. Unknown style → print an error to stderr and exit non-zero.\n   ```\n\n4. **Resume.**\n\n   ```sh\n   odin resume 005-add-greeting-styles\n   ```\n\n   Odin prepends your answers as `## Answers to prior questions` to the\n   original task body and moves the task back to `queue/pending/`.\n\n5. **Continue.**\n\n   ```sh\n   odin run --platform claude\n   ```\n\n   Task 005 runs in a fresh Claude session with your answers, then 006\n   adds tests, then 007 writes the README.\n\n## Inspect\n\nAt any point:\n\n```sh\nodin status\n```\n\nprints how many tasks are in each subdirectory and the next pending\nslug.\n\n## Resetting the demo\n\nThe demo is designed to be re-run. To start over:\n\n```sh\n# discard the agent\'s work (uncommitted files only; this folder is not\n# meant to be a git repo for the demo)\nrm -rf src tests pyproject.toml uv.lock .venv README.md\n\n# put the queue back to its pristine state\nrm -rf queue/running queue/done queue/failed queue/held queue/carry\n# leave queue/pending/ alone — those are the source-of-truth tasks\n```\n\nIf you\'ve already moved any tasks out of `pending/` during a run, move\nthem back manually — Odin never deletes queue files, so the originals\nare wherever they were last routed.\n\n## Troubleshooting\n\n- **"claude: command not found"** — install Claude Code and ensure it\'s\n  on `PATH` in the shell Odin runs from.\n- **Task lands in `failed/`** — open it and the captured stderr/stdout\n  (Odin will have printed the path). Most common cause is the agent\n  forgetting to emit one of the sentinel blocks; the body in\n  `failed/` is the original task — fix the prompt or the CLAUDE.md\n  guidance and move it back to `pending/`.\n- **Bash keeps prompting** — add `Bash` (and the tools you actually\n  need) to `--allowed-tools`.\n\n---\n\n# greeter (the CLI)\n\n## What it is\n\n`greeter` is a tiny Python CLI that prints greetings. It is the\nthrowaway product that the Odin demo above builds task by task. It has\nno runtime dependencies (stdlib only), targets Python `>=3.11`, and\nsupports a handful of greeting styles selected with a `--style` flag.\n\n## Install\n\nThis is a `uv`-managed project — do not install against system Python.\nFrom the project root:\n\n```sh\nuv sync\n```\n\nThat creates the virtual environment and installs the `greeter` script\ndefined in `pyproject.toml`.\n\n## Usage\n\nRun the plain greeting:\n\n```sh\nuv run greeter World\n# Hello, World!\n```\n\nOne example per supported style (via `--style`):\n\n```sh\nuv run greeter World --style plain         # Hello, World!\nuv run greeter World --style formal        # Good day, World.\nuv run greeter World --style casual        # Hey World!\nuv run greeter World --style enthusiastic  # HELLO World!!!\n```\n\n## Project status\n\ndemo — built by Odin\n',
    'queue/pending/001-init-pyproject.md': '# Task: initialize pyproject.toml\n\nCreate `pyproject.toml` at the project root for a Python package named\n`greeter`.\n\nRequirements:\n\n- Build backend: `hatchling` (declared, but no runtime install needed for\n  the demo).\n- Python version: `>=3.11`.\n- Project name: `greeter`. Version: `0.0.1`. Description: one short line.\n- No runtime dependencies.\n- Console script: `greeter = "greeter.__main__:main"`.\n- Package source layout: `src/greeter`.\n\nDo not create any Python files in this task — only `pyproject.toml`.\n',
    'queue/pending/002-add-package-skeleton.md': '# Task: add the package skeleton\n\nCreate the package directory and empty module files:\n\n- `src/greeter/__init__.py` — set `__version__ = "0.0.1"` and nothing else.\n- `src/greeter/__main__.py` — define a `main()` function that prints\n  exactly `greeter: no-op` and exits 0. No argparse yet.\n\nDo not add `core.py` or any greeting logic in this task — that comes\nlater.\n',
    'queue/pending/003-add-hello-core.md': '# Task: add the greeting core function\n\nCreate `src/greeter/core.py` with a single function:\n\n```python\ndef hello(name: str) -> str:\n    ...\n```\n\n`hello("World")` must return the string `"Hello, World!"`. `hello("")`\nmust return `"Hello, friend!"` (empty name → default).\n\nNo I/O. No printing. Just the function. The CLI will call it later.\n',
    'queue/pending/004-wire-cli.md': '# Task: wire the CLI to call hello()\n\nReplace the placeholder `main()` in `src/greeter/__main__.py` so that:\n\n- It uses `argparse` (stdlib).\n- It accepts a single optional positional argument `name` (default: `World`).\n- It calls `greeter.core.hello(name)` and prints the result to stdout.\n- It exits 0 on success.\n\nSmoke-check manually by reading the code — no test file in this task.\n',
    'queue/pending/005-add-greeting-styles.md': '# Task: add greeting styles\n\nExtend the greeter so users can pick a *style* of greeting beyond plain\n"Hello, X!". Examples might include something formal, something casual,\nsomething enthusiastic — but the exact set, the names, the CLI surface,\nand how the default is chosen are **not specified** and matter for how\nthe rest of the project evolves.\n\nSpecifically, the following are NOT derivable from this task body, the\nexisting code, or `CLAUDE.md`:\n\n- Which styles to support, and what each one should output for a given\n  name.\n- How the user selects a style on the CLI (a `--style` flag with named\n  values? a subcommand per style? a random pick by default?).\n- What happens when a style name is unknown — error and exit non-zero,\n  or fall back to plain?\n\nTreat these as substantive decisions. Per the sentinel protocol, do not\nguess — emit `<<<NEEDS_INPUT>>>` with concrete options and the\ntrade-offs so the user can answer once, then a future run can implement.\n\nDo **not** modify any source files in this task.\n',
    'queue/pending/006-add-tests.md': '## Prior questions and the user\'s answers\n\nIn a previous run you asked the questions below and the user answered them. Treat these answers as resolved decisions and proceed with the task — do not re-ask.\n\n### Questions you asked\n\n### Question 1\n_Prior choice \'run implementation first\' can\'t be honored: task 005 was decision-only by design and is done, and NO task in the queue actually implements the styles — so 006 keeps re-running with core.py still only containing hello(name). This is the third cycle._\n\nHow should we break the deadlock so the style tests can be written against real code?\n\n- **a** — Queue a real implementation task (recommended): Odin adds an impl task that writes greet(name, style) + wires --style per the already-resolved decisions; 006 runs after it. Honors your repeated \'keep tasks separate\' choice; fixes the missing-task gap that caused the loop.\n- **b** — Implement styles in THIS task, then test: I add greet()/--style here per the resolved decisions, then write all tests in one run. Breaks the loop immediately but reverses your prior a-over-b (no-bundling) preference.\n- **c** — Test hello() only now: Write just the two hello() assertions, defer style tests. Partial vs. task scope.\n\nRecommended: a — Your decision was sound; it looped only because the implementation task never existed — naming and queuing it now honors your choice and lets 006 run cleanly.\n\n<details><summary>raw agent block</summary>\n\n```\n{\n  "questions": [\n    {\n      "problem": "Prior choice \'run implementation first\' can\'t be honored: task 005 was decision-only by design and is done, and NO task in the queue actually implements the styles — so 006 keeps re-running with core.py still only containing hello(name). This is the third cycle.",\n      "question": "How should we break the deadlock so the style tests can be written against real code?",\n      "options": [\n        {"key": "a", "label": "Queue a real implementation task", "detail": "Odin adds an impl task that writes greet(name, style) + wires --style per the already-resolved decisions; 006 runs after it. Honors your repeated \'keep tasks separate\' choice; fixes the missing-task gap that caused the loop."},\n        {"key": "b", "label": "Implement styles in THIS task, then test", "detail": "I add greet()/--style here per the resolved decisions, then write all tests in one run. Breaks the loop immediately but reverses your prior a-over-b (no-bundling) preference."},\n        {"key": "c", "label": "Test hello() only now", "detail": "Write just the two hello() assertions, defer style tests. Partial vs. task scope."}\n      ],\n      "recommended": "a",\n      "why": "Your decision was sound; it looped only because the implementation task never existed — naming and queuing it now honors your choice and lets 006 run cleanly."\n    }\n  ]\n}\n```\n\n</details>\n\n### User\'s answers\n\nQ1: How should we break the deadlock so the style tests can be written against real code?\n   → a) Queue a real implementation task\n\n---\n\n## Prior questions and the user\'s answers\n\nIn a previous run you asked the questions below and the user answered them. Treat these answers as resolved decisions and proceed with the task — do not re-ask.\n\n### Questions you asked\n\n### Question 1\n_Task 005-add-greeting-styles is in queue/done/ but never wrote code — src/greeter/core.py still only has hello(name). This test task (006) needs the styles to exist and forbids inventing them. You previously chose (a) \'run implementation first\' over (b) \'bundle\'._\n\nHow should the missing styles implementation be repaired so this test task can run as written?\n\n- **a** — Re-open/run task 005 first (recommended): Move 005 back to pending and actually implement greet(name, style) per its resolved decisions, then resume 006. Honors your prior (a) choice; keeps tasks focused.\n- **b** — Authorize bundling in 006 now: Reverse your earlier (a) choice: I implement the 4 styles in core.py + wire --style, then write tests, all in this task.\n- **c** — Test hello() only: Reduce 006 to assert hello(\'World\') and hello(\'\') just now, defer all style tests. Partial vs. task scope.\n\nRecommended: a — It matches your already-given (a) decision and keeps one-task-one-change; the test task then runs exactly as written.\n\n<details><summary>raw agent block</summary>\n\n```\n{\n  "questions": [\n    {\n      "problem": "Task 005-add-greeting-styles is in queue/done/ but never wrote code — src/greeter/core.py still only has hello(name). This test task (006) needs the styles to exist and forbids inventing them. You previously chose (a) \'run implementation first\' over (b) \'bundle\'.",\n      "question": "How should the missing styles implementation be repaired so this test task can run as written?",\n      "options": [\n        {"key": "a", "label": "Re-open/run task 005 first", "detail": "Move 005 back to pending and actually implement greet(name, style) per its resolved decisions, then resume 006. Honors your prior (a) choice; keeps tasks focused."},\n        {"key": "b", "label": "Authorize bundling in 006 now", "detail": "Reverse your earlier (a) choice: I implement the 4 styles in core.py + wire --style, then write tests, all in this task."},\n        {"key": "c", "label": "Test hello() only", "detail": "Reduce 006 to assert hello(\'World\') and hello(\'\') just now, defer all style tests. Partial vs. task scope."}\n      ],\n      "recommended": "a",\n      "why": "It matches your already-given (a) decision and keeps one-task-one-change; the test task then runs exactly as written."\n    }\n  ]\n}\n```\n\n</details>\n\n### User\'s answers\n\nQ1: How should the missing styles implementation be repaired so this test task can run as written?\n   → a) Re-open/run task 005 first\n\n---\n\n## Prior questions and the user\'s answers\n\nIn a previous run you asked the questions below and the user answered them. Treat these answers as resolved decisions and proceed with the task — do not re-ask.\n\n### Questions you asked\n\n### Question 1\n_This \'add tests\' task requires greeting styles to exist, but they were never implemented — core.py still only has hello(name), and the task forbids inventing styles._\n\nHow should I proceed given styles aren\'t implemented yet?\n\n- **a** — Run implementation task first (recommended): Stop here; queue/run the \'implement styles\' task, then this test task works as written. Cleanest, respects one-task-one-change.\n- **b** — Implement styles + tests together: I implement greet(name, style) per the already-resolved decisions in carry-context, then write all tests. Bundles two tasks into one.\n- **c** — Test hello() only now: Write tests/test_core.py covering just hello(\'World\') and hello(\'\'); defer style assertions. Partial vs. task scope.\n\nRecommended: a — Keeps tasks focused per CLAUDE.md; the style decisions are fully resolved, so the implementation task can run cleanly before tests.\n\n<details><summary>raw agent block</summary>\n\n```\n{\n  "questions": [\n    {\n      "problem": "This \'add tests\' task requires greeting styles to exist, but they were never implemented — core.py still only has hello(name), and the task forbids inventing styles.",\n      "question": "How should I proceed given styles aren\'t implemented yet?",\n      "options": [\n        {"key": "a", "label": "Run implementation task first", "detail": "Stop here; queue/run the \'implement styles\' task, then this test task works as written. Cleanest, respects one-task-one-change."},\n        {"key": "b", "label": "Implement styles + tests together", "detail": "I implement greet(name, style) per the already-resolved decisions in carry-context, then write all tests. Bundles two tasks into one."},\n        {"key": "c", "label": "Test hello() only now", "detail": "Write tests/test_core.py covering just hello(\'World\') and hello(\'\'); defer style assertions. Partial vs. task scope."}\n      ],\n      "recommended": "a",\n      "why": "Keeps tasks focused per CLAUDE.md; the style decisions are fully resolved, so the implementation task can run cleanly before tests."\n    }\n  ]\n}\n```\n\n</details>\n\n### User\'s answers\n\nQ1: How should I proceed given styles aren\'t implemented yet?\n   → a) Run implementation task first\n\n---\n\n# Task: add tests for core.hello and styles\n\nCreate `tests/test_core.py` covering:\n\n- `hello("World") == "Hello, World!"`\n- `hello("") == "Hello, friend!"`\n- One assertion per supported greeting style verifying its output for\n  `name="World"` (use whatever style set was implemented in the prior\n  task — read `src/greeter/core.py` to find them; do not invent new\n  styles here).\n\nRun the suite with `uv run pytest -q` and confirm green before\ncompleting. If `pytest` is not yet a declared dev dependency, add it to\n`pyproject.toml` under `[dependency-groups] dev` pinned to an exact\nversion, then `uv sync` and run.\n',
    'queue/pending/007-add-readme.md': '# Task: write the greeter README\n\nCreate `README.md` at the project root with these sections:\n\n- **What it is** — one paragraph.\n- **Install** — `uv` based install instructions (this is a uv-managed\n  project; do not suggest `pip install` against system Python).\n- **Usage** — show the plain `greeter World` invocation and one example\n  per supported style (read `src/greeter/core.py` to enumerate them).\n- **Project status** — single line: "demo — built by Odin".\n\nPull the project name and Python version from `pyproject.toml` — do not\ninvent.\n\nIf `README.md` already exists, emit `<<<NEEDS_INPUT>>>` asking whether\nto merge, replace, or skip.\n',
}
